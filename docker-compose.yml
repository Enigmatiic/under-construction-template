services:
  website:
    image: nginx:alpine
    container_name: under_construction_site
    volumes:
      # Maps your repo content directly into the Nginx web folder
      - .:/usr/share/nginx/html:ro
    ports:
      - "8080:80"    # Standard web traffic
    restart: unless-stopped

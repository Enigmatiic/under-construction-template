version: '3.8'

services:
  # Votre site web
  website:
    image: nginx:alpine
    container_name: site_under_construction
    volumes:
      - ./app:/usr/share/nginx/html
    restart: unless-stopped
